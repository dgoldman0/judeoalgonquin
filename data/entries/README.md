# Canonical entry source

This directory intentionally contains no language records yet. Future `*.jsonl` files are the reviewable source of truth and must validate against `schema/entry.schema.json`.

Legacy forms and generated examples belong in fixtures or creative references until they have been individually researched and reviewed. A SQLite database or embedding vector is always derived output and must not be committed here.
